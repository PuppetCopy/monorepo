{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo 'Skip build - using pre-built files'"
  },
  "deploy": {
    "startCommand": "bunx serve@latest dist -s -l $PORT",
    "restartPolicyType": "NEVER"
  }
}