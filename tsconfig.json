{
  // This configuration is used for local development and type checking.
  "extends": "./tsconfig.base.json",
  "include": [
    "middleware"
  ],
  "references": [],
  "compilerOptions": {
    
  },
  "exclude": [
    "node_modules",
  ]
}